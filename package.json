{
  "name": "schema-validator-agent",
  "version": "1.5.2",
  "type": "module",
  "main": "main.js",
  "description": "Automated schema validator for testing URLs against Schema.org and Google Rich Results Test",
  "scripts": {
    "start": "electron .",
    "start-local": "node scripts/local-server.js",
    "build:desktop": "electron-packager . SchemaTools --platform=win32 --arch=x64 --out=dist --overwrite",
    "build:web": "echo \"Skipping Electron build for web deployment\"",
    "build": "node -e \"process.exit(process.env.VERCEL || process.env.CI ? 0 : require('child_process').execSync('npm run build:desktop', {stdio:'inherit'}))\" || npm run build:web"
  },
  "keywords": [
    "schema",
    "validation",
    "seo",
    "schema.org",
    "google-rich-results"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^11.1.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "googleapis": "^144.0.0",
    "puppeteer": "^21.5.0"
  },
  "devDependencies": {
    "electron": "^39.1.0",
    "electron-packager": "^17.1.2"
  }
}
